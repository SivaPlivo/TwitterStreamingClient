# Uncomment the next line to define a global platform for your project

platform :ios, '9.0'

target 'StreamingClient' do
  use_frameworks!
    pod 'Kingfisher'
    pod 'EasyPeasy'

  # Pods for StreamingClient

  target 'StreamingClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StreamingClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
